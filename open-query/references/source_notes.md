# Source Notes

Source: `https://opendentalsoft.com:1943/ODQueryList/QueryList.aspx`

Snapshot captured locally for the `open-query` skill.

The source page contains Open Dental query examples for:

- MySQL 5.x
- MariaDB 10.x

The skill intentionally uses this local snapshot and ignores future changes unless the user explicitly asks to refresh it.

The page's own guidance says query examples were written for other practices, may need changes, and are sorted chronologically with newer queries later. Use examples as patterns, not as guaranteed final answers for every practice.
